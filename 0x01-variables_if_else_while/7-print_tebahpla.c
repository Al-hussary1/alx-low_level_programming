#include <stdio.h>

/**
 * main - Enty Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int alphabet;

	for (alphabet = 122; alphabet >= 97; alphabet--)
	{
		putchar (alphabet);
	}
	putchar ('\n');
return (0);
}
